from pydantic import BaseModel

class FinalizeIt(BaseModel):
    answer: str

system_prompts = {
    "FinalizeIt": (
        "Твоя задача — выдать финальный, готовый ответ на первоначальный запрос пользователя, "
        "основываясь на проделанных шагах решения (контексте). "
        "Не пиши свои промежуточные рассуждения в итоговый ответ. "
        "Выдай только чистый, финальный результат в поле answer."
    )
}